<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.weihan.ligong">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <!-- <uses-permission android:name="android.permission.GET_ACCOUNTS" /> -->
    <!-- <uses-permission android:name="android.permission.READ_PROFILE" /> -->
    <!-- <uses-permission android:name="android.permission.READ_CONTACTS" /> -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".LiGongApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".activities.LoginActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.HomeActivity" />
        <activity android:name=".activities.SettingsActivity" />
        <activity android:name=".activities.Func0Activity" />
        <activity android:name=".activities.Func8Activity" />
        <activity android:name=".activities.Func11Activity" />
        <activity android:name=".activities.Func9Activity" />
        <activity android:name=".activities.Func12Activity" />
        <activity android:name=".activities.Func13Activity" />
        <activity android:name=".activities.Func3Activity" />
        <activity android:name=".activities.ChooseListActivity" />
        <activity android:name=".activities.Func1Activity" />
        <activity android:name=".activities.Func4Activity"></activity>
    </application>

</manifest>